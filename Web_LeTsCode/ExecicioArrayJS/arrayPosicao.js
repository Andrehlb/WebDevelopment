/*var strings = 'Essa o Candido disse que é facinha!';
/*for(i = 0; i < Strings.length; i++) {
    console.log(Strings[i]);
}
Strings.sort();
for(i = 0; i < Strings.length; i++) {
    console.log(Strings[i]);
}

function ordemString(a, b) {
    return a > b ? 1 : a < b ? -1 : 0;
}

Strings.sort(ordemString);
for(i = 0; i < Strings.length; i++) {
    console.log(Strings[i] + " ");
}*//*
var subStrings = strings.substring(25) 
console.log(subStrings);
*/
let str1 = "Essa o Candido disse que é facinha!"
let str2 = "é facinha" , bool1 = true;

//if()

console.log(`A primeira string é: "${str1}"`);
console.log(`A segunda string "${str2}" está no final da primeira string, senão é ${!bool1}.`);